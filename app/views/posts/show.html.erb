<div class="post-detail">
  <p id="notice"><%= notice %></p>

  <p>Author : <%= @post.user.uname %></p>

  <h3><%= @post.name %></h3>

  <p>
    <%= raw @post.content %>
  </p>

    <% if current_user && @post.user == current_user %>
		<%= link_to 'Edit', edit_post_path(@post) %>
	<% end %>
</div>
